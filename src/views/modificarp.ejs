<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Registro Productos</title>

        <!-- Bootstrap 5.1 -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        

    </head>
    
    <body>
          
        <nav class="navbar navbar -dark bg-dark mb4">
            <a href="/" class="navbar-brand">Zona de modificacion</a>
        </nav>
        
        <div class="container">
            <h1> Datos de los Productos</h1>
            
        <div class="col-md7">   
            <table class=" table table-bordered table-hover">
                <thead>
                    <tr>     <!-- campos de la tabla -->
                        <th>N°</th>
                        <th>nombre</th>
                        <th>categoria</th>
                        <th>cantidad</th>
                        <th>descripcion</th>
                        <th>precio</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>  <!--para recorrer lo que viene de la DB -->
                    <% for(var i =0; i< productos.length; i++){ %>
                    <tr>  <!-- Para ir organizando en cada lugar de la tabla -->
                      <td><%=i+1%></td>
                        <td><%= productos[i].nombre%></td>
                        <td><%= productos[i].categoria%></td>
                        <td><%= productos[i].cantidad%></td>
                        <td><%= productos[i].descripcion%></td>
                        <td><%= productos[i].precio%></td>
                        <td>  <!-- el _id como idenditificador para buscar en la DB -->
                            <a href="/Eliminar/<%= productos[i]._id%>" class="btn btn-danger">Eliminar</a>
                            
                            <a href="/editar/<%= productos[i]._id%>" class="btn btn-info">Editar</a>


                        </td>

                      </tr>
                    </tr>
                    <%}%>
                </tbody>
        
            </table>    
        </div>
        </div>  
        
    </body>
</html>